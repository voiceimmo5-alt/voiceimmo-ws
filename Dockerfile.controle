FROM node:20-slim

WORKDIR /app

COPY package.json .
RUN npm install --omit=dev

COPY server.js .

ENV NODE_ENV=production
ENV SERVER_BASE_URL=https://controle.voxzen.io
ENV SERVICE_VERTICAL=CONTROLE_REGLEMENTAIRE

EXPOSE 8080

CMD ["node", "server.js"]
